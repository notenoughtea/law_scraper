name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - master
    # Запускается при любом коммите в main/master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      
      - name: Download dependencies
        working-directory: ./scraper
        run: |
          go mod download
          go mod verify

      - name: Run tests
        working-directory: ./scraper
        run: |
          go test -v ./...

      - name: Build
        working-directory: ./scraper
        run: |
          go build -v ./cmd/cron/main.go
          go build -v ./cmd/scraper/main.go

  deploy:
    name: Deploy to Server
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Записываем приватный ключ
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Добавляем хост в known_hosts
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts
          
          # Проверяем подключение
          echo "🔍 Проверяем SSH подключение..."
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "echo '✅ SSH подключение работает!'" || {
            echo "❌ Ошибка SSH подключения!"
            exit 1
          }

      - name: Create .env file
        run: |
          cat > .env << EOF
          API_URL=${{ secrets.API_URL }}
          PAGES_STORAGE=./data/pages.json
          MATCHED_DIR=./data/matched
          KEYWORDS=${{ secrets.KEYWORDS }}
          TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_SEND_AS_DOCUMENT=true
          CRON_SCHEDULE=${{ secrets.CRON_SCHEDULE }}
          RUN_ON_START=false
          PROJECT_ROOT=/app
          MAX_WORKERS=${{ secrets.MAX_WORKERS }}
          EOF
      
      - name: Create deployment info file
        run: |
          cat > .deployment_info << EOF
          COMMIT_HASH=${{ github.sha }}
          COMMIT_AUTHOR=${{ github.actor }}
          COMMIT_MESSAGE=$(git log -1 --pretty=format:'%s' 2>/dev/null || echo 'N/A')
          DEPLOY_DATE=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
          BRANCH=${{ github.ref_name }}
          WORKFLOW_RUN=${{ github.run_number }}
          EOF

      - name: Copy files to server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          set -e  # Прерывать при ошибках
          
          echo "📁 Создаем директории на сервере..."
          ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_HOST \
            "mkdir -p /opt/law_scraper/scraper /opt/law_scraper/data"
          
          echo "📦 Копируем файлы на сервер..."
          
          # Используем scp для копирования файлов (rsync может быть недоступен)
          # Копируем основные файлы и директории
          echo "   → Копируем scraper/..."
          scp -r -o StrictHostKeyChecking=no ./scraper/* $SERVER_USER@$SERVER_HOST:/opt/law_scraper/scraper/ || {
            echo "❌ Ошибка копирования scraper/"
            exit 1
          }
          
          echo "   → Копируем Dockerfile и docker-compose.yml..."
          scp -o StrictHostKeyChecking=no Dockerfile $SERVER_USER@$SERVER_HOST:/opt/law_scraper/ || true
          scp -o StrictHostKeyChecking=no docker-compose.yml $SERVER_USER@$SERVER_HOST:/opt/law_scraper/ || true
          
          echo "   → Копируем .env..."
          if [ -f .env ]; then
            scp -o StrictHostKeyChecking=no .env $SERVER_USER@$SERVER_HOST:/opt/law_scraper/.env || {
              echo "⚠️ Не удалось скопировать .env"
            }
          else
            echo "⚠️ Файл .env не найден, будет использован существующий на сервере"
          fi
          
          echo "   → Копируем .deployment_info..."
          if [ -f .deployment_info ]; then
            scp -o StrictHostKeyChecking=no .deployment_info $SERVER_USER@$SERVER_HOST:/opt/law_scraper/.deployment_info || {
              echo "⚠️ Не удалось скопировать .deployment_info"
            }
          fi
          
          echo "✅ Файлы скопированы!"
          
          # Проверяем что файлы скопировались
          echo "🔍 Проверяем наличие файлов на сервере..."
          ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_HOST \
            "ls -la /opt/law_scraper/scraper/go.mod && echo '✅ go.mod найден'" || {
            echo "❌ Ошибка: go.mod не найден на сервере!"
            exit 1
          }

      - name: Deploy with Docker Compose
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_HOST bash << 'ENDSSH'
            set -e  # Прерывать выполнение при ошибках
            set -x  # Показывать выполняемые команды
            
            cd /opt/law_scraper || {
              echo "❌ Не удалось перейти в /opt/law_scraper"
              exit 1
            }
            
            echo "📦 Проверяем и исправляем импорты в Go файлах..."
            if [ -d "scraper/internal" ]; then
              cd scraper
              find . -name "*.go" -type f -exec sed -i 's|lawScraper/scraper/internal|github.com/notenoughtea/law_scraper/internal|g' {} + 2>/dev/null || true
              find . -name "*.go" -type f -exec sed -i 's|"lawScraper/|"github.com/notenoughtea/law_scraper/|g' {} + 2>/dev/null || true
              cd ..
            fi
            
            echo "🛑 Останавливаем старые контейнеры..."
            docker-compose down || true
            
            echo "🗑️ Удаляем старые контейнеры и образы..."
            docker-compose rm -f || true
            docker system prune -f || true
            
            echo "🔨 Собираем новый образ..."
            if ! docker-compose build --no-cache; then
              echo "❌ Ошибка сборки Docker образа!"
              exit 1
            fi
            
            echo "🚀 Запускаем контейнеры..."
            if ! docker-compose up -d; then
              echo "❌ Ошибка запуска контейнеров!"
              docker-compose logs
              exit 1
            fi
            
            echo "⏳ Ждем запуска контейнеров (15 секунд)..."
            sleep 15
            
            echo "📊 Статус контейнеров:"
            docker-compose ps
            
            echo "📋 Последние логи:"
            docker-compose logs --tail=50
          ENDSSH

      - name: Verify deployment
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          sleep 5
          ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_HOST bash << 'ENDSSH'
            set -e
            
            cd /opt/law_scraper
            
            echo "🔍 Проверяем статус контейнеров..."
            if docker-compose ps | grep -q "Up"; then
              echo "✅ Deployment successful!"
              echo ""
              echo "📊 Статус контейнеров:"
              docker-compose ps
              echo ""
              echo "📋 Последние логи:"
              docker-compose logs --tail=30
            else
              echo "❌ Deployment failed!"
              echo ""
              echo "📊 Статус контейнеров:"
              docker-compose ps
              echo ""
              echo "📋 Полные логи:"
              docker-compose logs --tail=100
              exit 1
            fi
          ENDSSH

      - name: Notify on success
        if: success()
        run: |
          echo "🎉 Deployment to ${{ secrets.SERVER_HOST }} completed successfully!"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Deployment to ${{ secrets.SERVER_HOST }} failed!"

