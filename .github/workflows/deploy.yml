name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - master
    # Запускается при любом коммите в main/master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      
      - name: Download dependencies
        working-directory: ./scraper
        run: |
          go mod download
          go mod verify

      - name: Run tests
        working-directory: ./scraper
        run: |
          go test -v ./...

      - name: Build
        working-directory: ./scraper
        run: |
          go build -v ./cmd/cron/main.go
          go build -v ./cmd/scraper/main.go

  deploy:
    name: Deploy to Server
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Create .env file
        run: |
          cat > .env << EOF
          API_URL=${{ secrets.API_URL }}
          PAGES_STORAGE=./data/pages.json
          MATCHED_DIR=./data/matched
          KEYWORDS=${{ secrets.KEYWORDS }}
          TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_SEND_AS_DOCUMENT=true
          CRON_SCHEDULE=${{ secrets.CRON_SCHEDULE }}
          RUN_ON_START=false
          PROJECT_ROOT=/app
          MAX_WORKERS=${{ secrets.MAX_WORKERS }}
          EOF

      - name: Copy files to server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          # Создаем директорию на сервере
          ssh $SERVER_USER@$SERVER_HOST "mkdir -p /opt/law_scraper/{scraper,data}"
          
          # Исключаем ненужные файлы и копируем через rsync
          rsync -avz --progress \
            --exclude '.git' \
            --exclude 'node_modules' \
            --exclude '.env' \
            --exclude 'main' \
            --exclude '*.md' \
            ./ $SERVER_USER@$SERVER_HOST:/opt/law_scraper/
          
          # Копируем .env отдельно
          scp .env $SERVER_USER@$SERVER_HOST:/opt/law_scraper/.env || echo "⚠️ .env не найден, будет использован существующий на сервере"

      - name: Deploy with Docker Compose
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          ssh $SERVER_USER@$SERVER_HOST << 'ENDSSH'
            set -e  # Прерывать выполнение при ошибках
            
            cd /opt/law_scraper
            
            echo "📦 Проверяем и исправляем импорты в Go файлах..."
            cd scraper
            # Убеждаемся что импорты правильные (на случай если были старые)
            if [ -d "internal" ]; then
              find . -name "*.go" -type f -exec sed -i 's|lawScraper/scraper/internal|github.com/notenoughtea/law_scraper/internal|g' {} + 2>/dev/null || true
              find . -name "*.go" -type f -exec sed -i 's|"lawScraper/|"github.com/notenoughtea/law_scraper/|g' {} + 2>/dev/null || true
            fi
            cd ..
            
            echo "🛑 Останавливаем старые контейнеры..."
            docker-compose down || true
            
            echo "🗑️ Удаляем старые образы..."
            docker-compose rm -f || true
            docker image prune -f || true
            
            echo "🔨 Собираем новый образ..."
            docker-compose build --no-cache
            
            echo "🚀 Запускаем контейнеры..."
            docker-compose up -d
            
            echo "⏳ Ждем запуска контейнеров (10 секунд)..."
            sleep 10
            
            echo "📊 Статус контейнеров:"
            docker-compose ps
            
            echo "📋 Последние логи:"
            docker-compose logs --tail=50
          ENDSSH

      - name: Verify deployment
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          sleep 10
          ssh $SERVER_USER@$SERVER_HOST << 'ENDSSH'
            cd /opt/law_scraper
            if docker-compose ps | grep -q "Up"; then
              echo "✅ Deployment successful!"
              docker-compose ps
            else
              echo "❌ Deployment failed!"
              docker-compose logs --tail=100
              exit 1
            fi
          ENDSSH

      - name: Notify on success
        if: success()
        run: |
          echo "🎉 Deployment to ${{ secrets.SERVER_HOST }} completed successfully!"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Deployment to ${{ secrets.SERVER_HOST }} failed!"

